<div class="flex flex-col items-center p-20">
  <h1 class="font-bold text-4xl">Available Rides:</h1>

  <div class="grid p-4 text-lg gap-4">
    <% @available_rides.each do |ride|%>
      <%= render 'available_ride', ride:ride %>
    <% end %>
  </div>
  
</div>
